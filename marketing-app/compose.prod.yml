services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: odissey-website:latest
    restart: unless-stopped
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
